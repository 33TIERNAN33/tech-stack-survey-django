Place Django JSON fixture files in this folder to have setup.sh load them after migrations.

Each setup run also writes a timestamped snapshot of the current database here before it updates and restarts the application.
